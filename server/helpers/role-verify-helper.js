'use strict';

export default class RoleVerify {
    static async getRole (id) {
        return new Promise((resolve, reject) => {
            //find in database and get the role here
            resolve(null);
        });
    }
}